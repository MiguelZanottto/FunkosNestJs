import { UuidIdValidatorPipe } from './uuid-id-validator.pipe';

describe('UuidIdValidatorPipe', () => {
  it('should be defined', () => {
    expect(new UuidIdValidatorPipe()).toBeDefined();
  });
});
