import { NumericIdValidatorPipe } from './numeric-id-validator.pipe';

describe('NumericIdValidatorPipe', () => {
  it('should be defined', () => {
    expect(new NumericIdValidatorPipe()).toBeDefined();
  });
});
